For governed company or product work, use the `company` skill installed at `.claude/skills/company/SKILL.md`. Install it with `companymd install . --agent claude` if needed.

Keep business facts in the selected Company.md pack. The skill resolves a requested subject through `companymd.yaml`, validates one task-specific context, and selects explicit design/template bindings. Follow the repository's existing workflow instructions as well; no `AGENTS.md` import is required for this adapter.
