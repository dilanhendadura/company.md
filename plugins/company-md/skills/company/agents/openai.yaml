interface:
  display_name: "Company.md"
  short_description: "Create work from approved company context"
  brand_color: "#172033"
  default_prompt: "Use $company to create a sales deck for this customer using the approved company and design context."
policy:
  allow_implicit_invocation: true
